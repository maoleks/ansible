---
- name: Install Nginx and deploy rainbow page with image
  hosts: web
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install Nginx
      apt:
        name: nginx
        state: present

- name: Download NC image from Google Drive
  get_url:
    url: https://drive.google.com/uc?export=download&id=1TUqmpBtqPBnvKySgft8M-BDKA-t0yIdU
    dest: /var/www/html/nc.png
    mode: '0644'

    - name: Create index.html with rainbow and North Carolina
      copy:
        dest: /var/www/html/index.html
        content: |
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <title>Hello North Carolina</title>
            <style>
              body {
                text-align: center;
                font-family: Arial, sans-serif;
                background: linear-gradient(to right, red, orange, yellow, green, blue, indigo, violet);
                color: white;
                height: 100vh;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
              }
              h1 {
                font-size: 60px;
                text-shadow: 2px 2px 4px black;
              }
              img {
                max-width: 80%;
                height: auto;
                border: 5px solid white;
                margin-top: 20px;
              }
            </style>
          </head>
          <body>
            <h1>HELLO NORTH CAROLINA!</h1>
            <img src="nc.png" alt="North Carolina">
          </body>
          </html>
        owner: www-data
        group: www-data
        mode: '0644'

    - name: Ensure Nginx is running
      systemd:
        name: nginx
        state: started
        enabled: yes
